{
  "private": true,
  "scripts": {
    "lint": "tslint --config ../tslint.json --project ../tsconfig.settings.json",
    "compile": "tsc --version && tsc -b ../src",
    "compile-release": "tsc -b ../tsconfig.release.json",
    "compile-tests": "tsc -b ../tests",
    "check-dts-config": "tsc --lib es2015 --allowJs --noEmit ../dts-config.js"
  },
  "devDependencies": {
    "tslint": "~5.12.0",
    "typescript": "~3.2.1"
  }
}
